#include<iostream>
#include<bits/stdc++.h>
using namespace std;
   int main(){
    int a,b;
    cin>>a>>b;
   b%=(a*(a+1))/2;
     for(int i=1;i<=a;i++){
         if(b<i){
             break;
         }
         else{
             b-=i;
         }
     }
     cout<<b;
    return 0;
}
